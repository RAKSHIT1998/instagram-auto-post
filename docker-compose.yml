version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
    environment:
      RUN_EMBEDDED_WORKER: "false"
      RUN_SCHEDULER: "false"
      RUN_ANALYTICS_CRON: "false"
      CORS_ORIGIN: http://localhost:5173
      MONGO_URI: mongodb://mongo:27017/social_ai
      REDIS_URL: redis://redis:6379
      ML_SERVICE_URL: http://ml:8000
    restart: unless-stopped
    depends_on:
      - redis
      - mongo

  worker:
    build:
      context: .
      dockerfile: worker/Dockerfile
    env_file:
      - ./backend/.env
    environment:
      MONGO_URI: mongodb://mongo:27017/social_ai
      REDIS_URL: redis://redis:6379
      ML_SERVICE_URL: http://ml:8000
    restart: unless-stopped
    depends_on:
      - redis
      - mongo

  ml:
    build:
      context: ./ml-service
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    environment:
      VITE_API_BASE: http://localhost:5000
    ports:
      - "5173:5173"
    restart: unless-stopped
    depends_on:
      - backend

  redis:
    image: redis:7
    restart: unless-stopped

  mongo:
    image: mongo
    ports:
      - "27017:27017"
    restart: unless-stopped
